# Copy this file to ~/.gpt-image-2/.env and fill in your key.
# The MCP server reads this file on startup.

OPENAI_API_KEY=sk-...

# Optional overrides
# GPT_IMAGE_MODEL=gpt-image-alpha
# GPT_IMAGE_OUTPUT_DIR=/path/to/save/images
